{
  "title": "Ingress Anomaly Event",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": ["historical", "future"]
    },
    "series": { "type": "string" },
    "date": { "type": "string", "format": "date-time" },
    "city": { "type": "string" },
    "region": { "type": "string" },
    "country": { "type": "string" },
    "winner": {
      "type": "string",
      "enum": ["enl", "res", "tie", "unknown"]
    },
    "score": { "type": "number" },
    "lat": { "type": "number" },
    "lng": { "type": "number" }
  },
  "required": ["type", "series", "date", "city", "country"]
}